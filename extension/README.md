# Sheltermark Chrome Extension

## Loading into Chrome

1. Open Chrome and navigate to `chrome://extensions/`
2. Enable "Developer mode" (toggle in top right)
3. Click "Load unpacked"
4. Select the `extension` folder

## Development

After making changes to any file, simply reload the extension from `chrome://extensions/`
